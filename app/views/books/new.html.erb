<%= f.file_field :image, accept: "image/*" %>
<h1>New book</h1>
<%= form_with model: @post_image do |f| %>
  <h4>Title</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>Opinion</h4>
  <%= f.text_area :caption %>
  <%= f.submit '投稿' %>
<% end %>